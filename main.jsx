import React from "react";
import ReactDOM from "react-dom/client";
import MovieLeague from "./MovieLeague";
import "./style.css";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <MovieLeague />
  </React.StrictMode>
);
